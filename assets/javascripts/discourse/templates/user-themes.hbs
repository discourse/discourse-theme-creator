<div class="user-theme-creator">
  {{#unless editingTheme}}
  <div class='content-list theme-list'>
    <h3>{{i18n 'theme-creator.my_themes'}}</h3>
    <ul>
      {{#each model as |theme|}}
        <li>
          {{#link-to 'user.themes.show' theme replace=true}}
            {{theme.name}}
          {{/link-to}}
        </li>
      {{/each}}
    </ul>
    <p>{{d-button label="theme-creator.new_theme" icon="plus" action="newTheme" class="btn-primary"}}</p>
    <p>{{d-button action="editLocalModal" icon="pencil" label="theme-creator.edit_local"}}</p>
    <p>{{d-button action="importModal" icon="upload" label="admin.customize.import"}}</p>
    
  </div>
  {{/unless}}
  <div class="theme-details">
  {{outlet}}
  </div>
</div>
